name: Test Expressions

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test expression (not secret)
        env:
          TEST_VAR: ${{ github.workspace }}
        run: echo "Workspace: $TEST_VAR"
