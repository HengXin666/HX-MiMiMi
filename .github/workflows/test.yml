name: Test Secrets

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test with FAKE secret
        env:
          FAKE: ${{ secrets.FAKE_SECRET_12345 }}
        run: echo "FAKE secret: [$FAKE]"
