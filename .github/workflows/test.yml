name: Test Actions

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test with write permission only"
